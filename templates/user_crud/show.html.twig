{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <!-- ... Affichez les détails de l'utilisateur ... -->
    </table>

    <a href="{{ path('app_user_crud_index') }}">Back to list</a>
    <a href="{{ path('app_user_crud_edit', {'id': user.id}) }}">Edit</a>

    <!-- Bouton de suppression avec identifiant unique -->
    <button class="btn btn-danger" onclick="deleteUser({{ user.id }})">Delete</button>

    {{ include('user_crud/_delete_form.html.twig') }}

<!-- Script pour gérer l'appel AJAX -->

{% endblock %}